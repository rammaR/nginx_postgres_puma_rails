<%= form_with(model: mining_type, local: true) do |form| %>
  <% if mining_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mining_type.errors.count, "error") %> prohibited this mining_type from being saved:</h2>

      <ul>
      <% mining_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-3">
    <%= form.label :name, class:'form-label' %>
    <%= form.text_field :name, id: :mining_type_name, class: 'form-control' %>
  </div>

  <div class="mb-3">
    <%= form.label :acronym, class:'form-label' %>
    <%= form.text_field :acronym, id: :mining_type_acronym, class: 'form-control' %>
  </div>
  <hr>
  <div class="actions">
    <%= form.submit nil, class: 'btn btn-secondary' %>
  </div>
<% end %>
